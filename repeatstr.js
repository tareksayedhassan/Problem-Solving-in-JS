// Crate Empty string
//  Take the Number Arument and Loop Through
// add the string to the Empty one
function repeatestr(num, str) {
  return str.repeat(num);
  //   let newstring = "";
  //   for (let i = 0; i < num; i++) {
  //     newstring += str;
  //   }
  //   return newstring;
}
console.log(repeatestr(9, " t"));
